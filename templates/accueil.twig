<div class="container-fluid">

<header style="position: fixed; top: 0; left: 0; width: 100%; 
               display: flex; justify-content: space-between; align-items: center; 
               padding: 10px 20px; background-color: #f8f9fa; border-bottom: 1px solid #ccc; 
               z-index: 1000;">

    <!-- Bloc gauche : logo au-dessus + citation en dessous -->
    <div style="display: flex; flex-direction: column; align-items: flex-start;">
<img src="logos/Logo.png" 
     alt="Brackito - logiciel de gestion de tournois sportifs" 
     style="height: 50px; max-height: 40px; width: auto;" 
     class="img-fluid">

    <div style="font-size: 0.8em; color: gray; font-style: italic; margin-top: 5px;">
            The french brackets software
        </div>
    </div>

    <!-- Bloc droit : boutons -->
    <div class="d-flex gap-2">
        <a href="register.php" class="btn btn-outline-primary me-2">
            <i class="fas fa-user-plus me-1"></i> {{t('inscription')}}
        </a>
        <a href="Auth/" class="btn btn-primary">
            <i class="fas fa-sign-in-alt me-1"></i> {{t('login')}}
            
        </a>
    </div>
</header>

<!-- Décalage pour que le contenu ne passe pas sous le header -->
<div style="height:80px;"></div>

<div class="row shadow bg-light">
    <!-- Colonne gauche : texte -->
    <div class="col-12 col-lg-6 d-flex align-items-center justify-content-center p-4">
        <div class="text-center text-dark">
            <h1 class="mb-4 text-primary">{{t('titreIndex')}}</h1>
            <p class="lead mb-4">
            {{t('sousTitreIndex')}}
            </p>
            <div class="text-start fs-5">
                <p><i class="fas fa-users-gear me-2 text-primary"></i>{{t('indexCreationDePoules')}}</p>
                <p><i class="fas fa-calendar-alt me-2 text-success"></i>{{t('indexPlanification')}} </p>
                <p><i class="fas fa-trophy me-2 text-warning"></i>{{t('indexSuivi')}} </p>
                <p><i class="fas fa-user-slash me-2 text-danger"></i>{{t('indexGestionDesAbsences')}} </p>
                <p><i class="fas fa-mobile-screen-button me-2 text-info"></i> {{t('indexInterfaceMobile')}}</p>
                <p> 🤝 {{t('indexGestionDesPartenaires')}}</p>
                <p><i class="fas fa-layer-group me-2 text-secondary"></i>{{t('indexGestionCategories')}} </p>
                <p><i class="fas fa-circle-info me-2 text-dark"></i> <strong>{{t('indexTypePoule')}}</strong></p>
            </div>
        </div>
    </div>

    <!-- Colonne droite : vidéo -->
<div class="col-12 col-lg-6 d-flex align-items-center justify-content-center position-relative overflow-hidden" style="min-height:300px;">
        <!-- Vidéo en arrière-plan -->
       <video autoplay muted loop playsinline 
       poster="videos/HBCAT_Tournoi_herbe.jpg"
       class="position-absolute w-100 h-100 object-fit-cover">
   <source src="videos/HBCAT_Tournoi_herbe.webm" type="video/webm">
   <source src="videos/HBCAT_Tournoi_herbe_web.mp4" type="video/mp4">
   Ton navigateur ne supporte pas la vidéo.
</video>


        <!-- Logo en haut à droite -->
        <img src="logos/Logo.png" 
            class="position-absolute rounded img-fluid fade-in" 
            alt="Brackito - logiciel de gestion de tournois sportifs"
            style="z-index:1; top:10px; right:10px; width:120px; height:auto;">

        <!-- Texte explicatif -->
        <div class="position-absolute text-end p-3 rounded" 
            style="bottom:20px; right:10px; max-width:320px; background:rgba(255,255,255,0.8); z-index:1;">
            <small>
                {{t('indexCaptionVideo1') }}
                
            </small>
        </div>
    </div>
</div>



