<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Placez vos rencontres</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    
    <script src="https://unpkg.com/htmx.org@1.9.12" integrity="sha384-ujb1lZYygJmzgSwoxRggbCHcjc0rB2XoQrxeTUQyRjrOnlCoYta87iKBWq3EsdM2" crossorigin="anonymous"></script>
  

  
  

    <style>
        .hidden {
            display: none;
        }
        #filter-input {
            position: fixed;
            top: 10px;
            right: 10px;
            padding: 5px;
            z-index: 1000;
            width: 150px;
            font-size: 12px;
        }

   

    </style>






</head>




<body>
    {{ include('header.twig') }}
    <div class="row ml-n4">
        <div class="col-lg-3">
            {% for tournoi in ListeDesTournois %}
            {% if tournoi.isArchived == 0 %}
            {% if tournoi.id == idTournoi %}
            <a href="PlacementDesRencontres.php?id_tournoi={{ tournoi.id }}" class="btn btn-danger">{{ tournoi.nom }} <i class="fas fa-trophy"></i></a>
            {% else %}
            <a href="PlacementDesRencontres.php?id_tournoi={{ tournoi.id }}" class="btn btn-primary btn-sm ">{{ tournoi.nom }} <i class="fas fa-trophy"></i></a>
            {% endif %}
            {% endif %}
            {% endfor %}
        </div>
        <div class="col-lg-12">
           <div class="container my-4">

  <div class="card shadow-sm  {% if AfficherLesEquipes['nombre'] >= licence.limite_equipes and today > dateFin %} d-none {% endif %}">
    <div class="card-body">

      <!-- Rangée flex : chaque formulaire est un "bloc" côte-à-côte -->
      <div class="d-flex align-items-center flex-wrap gap-3">

        <!-- 1) FORMULAIRE D’ACTION GROUPÉE ------------------------------- -->
        <form id="bulkForm"
              action="PlacementDesRencontresTraitement.php"
              method="post"
              class="d-inline-flex align-items-center gap-2">

          <input type="hidden" name="idTournoi" value="{{ idTournoi }}">

          <label for="bulkAction" class="mb-0">{{t('actionGroupee')}}</label>

          <select name="action"
                  id="bulkAction"
                  class="form-select form-select-sm"
                  required>
            <option value="" disabled selected>{{t('actionGroupee')}}</option>
            <option value="deplanifier">{{t('deplanifierCesRencontres')}}</option>
          </select>

          <button type="submit" class="btn btn-outline-primary btn-sm">
            {{t('valider')}}
          </button>
        </form>
        <!-- /fin bulkForm -->


        <!-- 2) FORMULAIRE MODIFICATION DES HORAIRES  -->
        <form action="PlacementDesRencontresTraitement.php"
              method="post"
              class="d-inline-flex align-items-center flex-wrap gap-3">

          <!-- Retarder les rencontres -->
          <div class="d-flex align-items-center">
            <label for="modifMinutes" class="form-label mb-0 me-2">
              {{t('retarderLesRencontres')}}&nbsp;:
            </label>
            <input type="number"
                   id="modifMinutes"
                   name="modifMinutes"
                   placeholder="Minutes"
                   class="form-control form-control-sm"
                   style="width: 100px;"
                   onfocusout="this.form.submit()">
          </div>

          <!-- Ajuster le pas horaire -->
          <div class="d-flex align-items-center">
            <label for="modifPasHoraire" class="form-label mb-0 me-2">
              {{t('dureeRencontre')}}:
            </label>
            <input type="number"
                   id="modifPasHoraire"
                   name="modifPasHoraire"
                   min="0"
                   value="{{ tournoiInfo['pasHoraire'] }}"
                   class="form-control form-control-sm"
                   style="width: 100px;"
                  oninput="this.value = this.value < 0 ? 0 : this.value;"  

                   onchange="this.form.submit()">
          </div>

          <!-- Champs cachés communs -->
          <input type="hidden" name="modifHeureDebut" value="{{ tournoiInfo['heure_debut'] }}">
          <input type="hidden" name="idTournoi" value="{{ idTournoi }}">
        </form>
        <!-- /fin deuxième formulaire  -->
 <div class="d-flex align-items-center">
                                    <i class="bi bi-info-circle-fill me-2 text-info"></i>
                                    <span><strong><a href="GererLabels.php?id_tournoi={{idTournoi}}">{{t('labels')}} :</a></strong> {{nombreDeLabel | length}}</span>
                                </div>
      </div><!-- /.d-flex -->
      
  <div class="d-flex align-items-center mt-3 gap-4">
                                <div class="d-flex align-items-center">
                                    <i class="bi bi-calendar-check me-2 text-info"></i>
                                    <span><strong>{{t('planifiee')}} :</strong> {{ nombreDeRencontresPlanifiee }}</span>
                                </div>
                                <div class="d-flex align-items-center">
                                    <i class="bi bi-list-ol me-2 text-success"></i>
                                    <span><strong>{{t('total')}} :</strong> {{ nombreRencontreAPlanifier }}</span>
                                </div>
                                <div class="d-flex align-items-center">
                                    <i class="bi bi-exclamation-circle me-2 text-warning"></i>
                                    <span><strong>{{t('restantes')}} :</strong> {{ nombreRencontreAPlanifier - nombreDeRencontresPlanifiee }}</span>
                                </div>
                                     <div class="d-flex align-items-center">

                               
                                    </div>
                                <div class="d-flex align-items-center">
    <a href="#"
       data-bs-toggle="tooltip"
       data-bs-placement="top"
       title="Le temps estimé est calculé en multipliant le nombre de rencontres à planifier par le pas horaire du tournoi et le tout divisé par le nombre de terrain">
        <i class="bi bi-clock me-2 text-info"></i>
    </a>
    <span><strong>{{t('tempsEstimeDuTournoi')}} :</strong>
        {% set timeInMinutes = (nombreRencontreAPlanifier * tournoiInfo.pasHoraire) / terrains|length %}
        {% set hours = timeInMinutes // 60 %}
        {% set minutes = timeInMinutes % 60 %}
        {{ hours }} {{t('heures')}} {{ minutes }} {{t('minutes')}}
    </span>      
    </div>
  </div>
</div>


                               
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
       

{% set dateFin = licence.date_fin|date('Y-m-d')|date %}
  {% set today = "now"|date('Y-m-d')|date %}


        
        <table class="table ms-1 {% if AfficherLesEquipes['nombre'] >= licence.limite_equipes and today > dateFin %} d-none {% endif %}" id="planification-table">
            <thead>
                <tr>
                    <th scope="col">{{t('heureDesRencontres')}}</th>
                                        {% for i in 0..terrains|length - 1 %}
                     <th scope="col" class="sticky-top bg-white" style="z-index: 1;">
                        <form id="terrain{{i}}">
                            <input type="hidden" name="terrain_id" value="{{terrains[i]['terrain_id']}}">
                            <input type="text" class="form-control form-control-sm" value="{{terrains[i]['nom']}}" name="nomTerrain"
                                maxlength="10"
                                hx-post="PlacementDesRencontresTraitement.php"
                                hx-trigger="change"
                                hx-include="#terrain{{i}} [name='terrain_id'], #terrain{{i}} [name='nomTerrain']"
                            >
                            <a href="PlacementDesRencontresTraitement.php?action=delTerrain&terrain_id={{terrains[i]['terrain_id']}}&idTournoi={{idTournoi}}">
                                <p class="text-danger"><i class="fa fa-minus-circle fa-sm" aria-hidden="true"></i></p>
                            </a>
                        </form>
                    </th>

                    {% endfor %}
                    <th scope="col"><a href="PlacementDesRencontresTraitement.php?action=addTerrain&idTournoi={{idTournoi}}"><h2><p class="text-success"><i class="fa fa-plus-circle" aria-hidden="true"></i></p></h2></a></th>
                </tr>
            </thead>
            <tbody>
                {% for creneau in listCreneaux %}
                <tr>
                    <td class="col-md-1 align-middle text-center">
                        <div class="d-flex flex-column align-items-center gap-1">

                            <!-- Bouton supprimer (en haut) -->
                            <a href="PlacementDesRencontresTraitement.php?action=delCreneau&creneauId={{creneau.creneau_id}}&tournoiId={{idTournoi}}&pas={{tournoiInfo.pasHoraire}}"
                            data-bs-toggle="tooltip"
                            data-bs-placement="top"
                            title="{{t('supprimerCreneau')}}"
                            class="text-danger">
                                <i class="fa fa-minus-circle fa-lg" aria-hidden="true"></i>
                            </a>

                            <!-- Champ modifiable de l'heure -->
                            <input type="text" name="creneau" class="form-control text-center p-1"
                                value="{{ creneau.nom|slice(0, -3) }}" size="4"
                                hx-post="PlacementDesRencontresTraitement.php"
                                hx-trigger="change"
                                hx-target="this"
                                hx-vals='{"creneau_id": "{{ creneau.creneau_id }}", "action": "updateCreneau"}'
                                pattern="^([01]\d|2[0-3]):[0-5]\d$"
                                title="Veuillez entrer une heure au format HH:MM (par exemple, 09:12 ou 14:30)."
                            >

                            <!-- Champ caché -->
                            <input type="hidden" name="idTournoi" value="{{idTournoi}}">

                            <!-- Bouton ajouter (en bas) -->
                            <a href="PlacementDesRencontresTraitement.php?action=ajouterCreneauEntre&idTournoi={{idTournoi}}&CreneauOrdre={{ creneau.ordre }}&pas={{tournoiInfo.pasHoraire}}"
                            data-bs-toggle="tooltip"
                            data-bs-placement="bottom"
                            title="{{t('insererCreneau')}}"
                            class="text-success">
                                <i class="fa fa-plus-circle fa-lg" aria-hidden="true"></i>
                            </a>
                        </div>
                    </td>

                    {% set Displayed = false %}
                    {% for j in 0..terrains|length - 1 %}


                    {# Trouver la planification correspondante #}
                       {% set couleurDeFond = '#ffffff' %} {# Valeur par défaut #}
                            {% for planification in afficherPlanification %}
                                {% if planification.terrain_id == terrains[j]['terrain_id'] and planification.creneau_id == creneau.creneau_id %}
                                    {% if planification.label_couleur is not empty %}
                                        {% set couleurDeFond = planification.label_couleur %}
                                    {% else %}
                                        {% set couleurDeFond = planification.equipe1_categorie_couleur %}
                                    {% endif %}
                                {% endif %}
                            {% endfor %}



                    <td id="planif-{{ terrains[j]['terrain_id'] }}-{{ creneau.creneau_id }}"  style="background-color: {{ couleurDeFond }}" class="planif-item">

                    
                        {% set planificationFound = false %}
                        {% set creneauOqp = false %}
                        {% set maCouleurDeFond = planification.equipe1_categorie_couleur %}
                        {% for planification in afficherPlanification %}
                            {% if planification.terrain_id == terrains[j]['terrain_id'] and planification.creneau_id == creneau.creneau_id  %}
                                {% set planificationFound = true %}
                                {% if planification.rencontre_id is not null or planification.arbitre_id is not null or planification.label_id is not null   %}
                                    {% if planification.arbitre_id is not null and  tournoiInfo.gestionArbitres == 1 %}
                                    
                                     <div class="d-flex align-items-center bg-dark text-light p-2 rounded">
                                        <!-- Icône calendrier à gauche -->
                                        <a href="PlacementDesRencontresTraitement.php?action=noPlanifEvent&event={{ planification.planification_id }}&arbitre_id={{ planification.arbitre_id }}&tournoi_id={{ idTournoi }}#planif-{{ terrains[j]['terrain_id'] }}-{{ creneau.creneau_id }}"
                                            class="text-danger me-3">
                                            <i class="fas fa-calendar-times fa-lg"></i>
                                        </a>

                                        <!-- Texte arbitre -->
                                        <p class="mb-0">
                                            <i class="fas fa-user-tie"></i>
                                            <i class="material-icons me-2" style="font-size: 20px;">sports</i>
                                            {{ planification.arbitre_nom }} {{ planification.club_nom_arbitre }}
                                        </p>
                                        </div>

                                    {% else %}
                                   {% if tournoiInfo.gestionArbitres == 1 %}
                                       <form action="PlacementDesRencontresTraitement.php" method="post" class="arbitreForm">
                                            <select class="form-select"  name="Addevent" id="listeArbitre">
                                             <option value="">{{t('choisirArbitre')}}</option>
                                                {% for arbitre in listeDesArbitres %}
                                                <option value='{"idterrain":"{{terrains[j]['terrain_id']}}","idcreneau":"{{creneau.creneau_id}}","idarbitre":"{{arbitre.arbitre_id}}","idtournoi":"{{idTournoi}}"}' >
                                                   {% if arbitre.nom is not empty %} {{t('arbitre')}}: {{arbitre.nom}} ({{arbitre.nombre_matchs}}) {% endif %}  {{t('club')}}: {{arbitre.club_nom}} ({{arbitre.nombre_matchs_club}})
                                                </option>
                                                {% endfor %}
                                            </select>
                                        </form>
                                     {% endif %}
                                    {% endif %}
                                    {% if planification.rencontre_id is not null  %}
                                        {% if planification.isClassement == 0 %}
                                            <p class="card-text">tour{{planification.tour}}-{{planification.equipe1_categorie_nom}}-{{planification.equipe1_nom_categorie}}{{planification.equipe1_nom}} vs {{planification.equipe2_nom}} -  {{ planification.equipe2_poule_noms|split(' - ')[1] }}  </p>
                                        {% elseif planification.isClassement == 1 %}
                                            <p class="card-text"><i class="fa fa-trophy" aria-hidden="true"></i> PF-tour{{planification.tour}}-{{planification.equipe1_categorie_nom}}-{{planification.equipe1_nom_categorie}}{{planification.equipe1_nom}} vs {{planification.equipe2_nom}} - {{ planification.equipe2_poule_noms|split(',')[0] }} </p>
                                        {% endif %}
                                          <!-- Groupe case à cocher + action planification -->
                            <div class="d-flex align-items-center gap-3">
                          

            <!-- La checkbox (cachée ou non) -->
            <input type="checkbox"
                class="form-check-input me-2"
                id="rencontreCheck{{ planification.planification_id }}"
                name="rencontreCheck[]"
                value="{{ planification.planification_id }}"
                form="bulkForm">

            <!-- (mets ici le texte ou les icônes que tu veux) -->
            {{ planification.label_description }}
       

  <!-- Le lien calendrier reste à droite et ne déclenche PAS le label -->
  <a href="PlacementDesRencontresTraitement.php?action=noPlanifEvent&event={{ planification.planification_id }}&tournoi_id={{ idTournoi }}&rencontreId={{ planification.rencontre_id }}#planif-{{ terrains[j]['terrain_id'] }}-{{ creneau.creneau_id }}"
     class="text-danger"
     data-bs-toggle="tooltip"
     title="Supprimer uniquement cette planification">
    <i class="fas fa-calendar-times fa-lg"></i>
  </a>
</div>



                                        {% set creneauOqp = true %}
                                    {% endif %}
                                {% endif %}  
                               {% if planification.label_id is not null %}
                                        
                                   <div class="d-flex align-items-center bg-dark text-warning p-2 rounded">
                                        <!-- Icône calendrier à gauche -->
                                        <a href="PlacementDesRencontresTraitement.php?action=noPlanifEvent&event={{ planification.planification_id }}&tournoi_id={{ idTournoi }}#planif-{{ terrains[j]['terrain_id'] }}-{{ creneau.creneau_id }}"
                                            class="text-danger me-3">
                                            <i class="fas fa-calendar-times fa-lg"></i>
                                        </a>

                                        <!-- Description du label -->
                                        <p class="mb-0">
                                            {{ planification.label_description }}
                                        </p>
                                        </div>

                            {% set creneauOqp = true %}

                                {% endif %}
                            {% endif %}
                        {% endfor %}
                        {% if creneauOqp == false %}
                            <form action="PlacementDesRencontresTraitement.php" method="post" class="rencontreLabel" >
                           
                                <select class="form-select basic"   name="Addevent" >
                                    <option value="">{{t('choisirRencontreOuLabel')}}</option>
                                    {% for planif in planificationSansCreneauNiTerrain %}
                                        {% if planif.isClassement == 0 %}
                                            <option value='{"idterrain":"{{terrains[j]['terrain_id']}}","idcreneau":"{{creneau.creneau_id}}","idrencontre":"{{planif.id}}","idtournoi":"{{idTournoi}}"}'>tour{{planif.tour}}-{{ planif.equipe1_categorie_nom }} {{planif.equipe1_nom}} vs {{planif.equipe2_nom}}  {{ planif.equipe1_poule_nom|split(' - ')[1] }} </option>
                                        {% elseif planif.isClassement == 1 %}
                                            <option value='{"idterrain":"{{terrains[j]['terrain_id']}}","idcreneau":"{{creneau.creneau_id}}","idrencontre":"{{planif.id}}","idtournoi":"{{idTournoi}}"}'>PF-tour{{planif.tour}}-{{ planif.equipe1_categorie_nom }} {{planif.equipe1_nom}} vs {{planif.equipe2_nom}} {{ planif.equipe1_poule_nom }} </option>
                                        {% endif %}
                                    {% endfor %}
                                    {% for label in libelleParTournoi %}
                                    
                                        <option value='{"idlabel":"{{label.label_id}}","idterrain":"{{terrains[j]['terrain_id']}}","idcreneau":"{{creneau.creneau_id}}","idtournoi":"{{idTournoi}}"}'>{{t('label')}}: {{label.description}}</option>
                                    {% endfor %}
                                </select>
                            </form>
                        {% endif %}
                    </td>
                    {% endfor %}
                </tr>
                {% endfor %}
            </tbody>
            <tfoot>
                <td>
                    <a id="add-creneau-link"
                       href="PlacementDesRencontresTraitement.php?action=addCreneau&idTournoi={{idTournoi}}&startTime={{lastCreneau['nom']|slice(0, -3)}}&newTime={{timeNextCreneau}}">
                        <h2><p class="text-success"><i class="fa fa-plus-circle" aria-hidden="true"></i></p></h2>
                    </a>
                    <span id="bottom"></span>
                </td>
            </tfoot>
        </table>
    </div>
    <input type="text" id="filter-input" placeholder="{{t('filtre')}}...">


         {% if AfficherLesEquipes['nombre'] >= licence.limite_equipes and today > dateFin %}  
         <div class="card shadow-sm">
            <div class="card-body">
                <div class="alert alert-danger" role="alert">
                    <strong>{{t('attention')}}</strong> {{t('limiteAtteinteMessage')}}.
                </div>
            </div>
         
         {% endif %}




{% if dateFin > today %}


    <div class="alert alert-warning mt-3">  
        <strong>Attention :</strong> La date de fin de votre licence est dépassée. Vous ne pouvez plus planifier de rencontres.     
        </div>
{% endif %}

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const filterInput = document.getElementById('filter-input');
            const storedFilter = localStorage.getItem('filterValue');
            if (storedFilter) {
                filterInput.value = storedFilter;
                filterOptions(storedFilter.toLowerCase());
            }

            filterInput.addEventListener('input', function() {
                const filter = this.value.toLowerCase();
                localStorage.setItem('filterValue', this.value);
                filterOptions(filter);
            });
        });

        function filterOptions(filter) {
            const selects = document.querySelectorAll('select');
            selects.forEach(select => {
                const options = select.querySelectorAll('option');
                options.forEach(option => {
                    if (option.text.toLowerCase().includes(filter)) {
                        option.classList.remove('hidden');
                    } else {
                        option.classList.add('hidden');
                    }
                });
            });
        }
    </script>

    <script>
        $(document).ready(function() {
            $('.rencontreLabel .form-select').on('change', function() {
                var $form = $(this).closest('form');
                var formAction = $form.attr('action');
                var formData = $form.serialize();

                $.post(formAction, formData, function() {
                    location.reload();
                });
            });
        });



           $(document).ready(function() {
        $('.arbitreForm .form-select').on('change', function() {
            var $form = $(this).closest('form');
            var formAction = $form.attr('action');
            var formData = $form.serialize();

            $.post(formAction, formData, function() {
                location.reload();
            });
        });
    });
    </script>

    <script>
        document.querySelectorAll('input[name="creneau"]').forEach(function(input) {
            input.addEventListener('input', function(e) {
                const value = e.target.value;
                const regex = /^([01]\d|2[0-3]):[0-5]\d$/;
                if (!regex.test(value) && value.length >= 5) {
                    alert('Please enter a valid time in HH:MM format (e.g., 09:12 or 14:30).');
                    e.target.value = value.slice(0, -1);
                }
            });
        });
    </script>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            console.log("DOM fully loaded and parsed");
            var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
            console.log("Tooltip elements:", tooltipTriggerList);
            var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
                console.log("Initializing tooltip for element:", tooltipTriggerEl);
                return new bootstrap.Tooltip(tooltipTriggerEl)
            })
        });
    </script>


<script>
  // Stocker la position de défilement avant de quitter la page
  window.onload = function() {
    if (typeof(sessionStorage) !== 'undefined') {
      var scrollPosition = sessionStorage.getItem('scrollPosition');
      if (scrollPosition !== null) {
        window.scrollTo(0, scrollPosition);
      }
    }
  };

  window.onbeforeunload = function() {
    if (typeof(sessionStorage) !== 'undefined') {
      sessionStorage.setItem('scrollPosition', window.scrollY);
    }
  };
</script>

<script>
document.querySelectorAll('.planif-item').forEach(item => {
  const checkbox = item.querySelector('input[type="checkbox"]');

  item.addEventListener('click', e => {
    // Si on clique sur le lien (ou son icône), on ne touche pas à la case
    if (e.target.closest('a')) return;

    checkbox.checked = !checkbox.checked;   // toggle
  });
});
</script>



    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.bundle.min.js"></script>
</body>
</html>

